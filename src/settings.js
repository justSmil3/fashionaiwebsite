import React, { useState } from 'react';

const Settings = () => {
  const [inputText, setInputText] = useState('');

  return (
    <div className="pr-4 flex items-center justify-center w-full h-full border border-gray-300 p-2 rounded-md text-white bg-gray-700">
        settings
    </div>
  );
};

export default Settings;
